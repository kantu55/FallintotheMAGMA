仮想関数
役割によって異なる動作をする関数
派生クラスのアドレスは基底クラスのメンバ変数に格納できる
基底クラスのポインタ変数に対し、アロー演算子を使用すると、そのクラスのメンバ変数を呼び出せる
このとき、通常のメンバ変数は、ポインタ変数で定義されたもの
つまり、基底クラスのメンバ変数が呼び出される
そこで、メンバ変数にvirtualを付けると派生クラスの関数が呼び出される。

純粋仮想関数
この仮想関数は、派生クラスで必ず定義しなければならない
仮想関数の末尾に「＝0」をつけると純粋仮想関数になる。
また、純粋仮想関数を持つクラスは、そのクラスの変数を定義できない。
